{
  "name": "gun-brain",
  "version": "0.1.0",
  "private": true,
  "description": "AiA Central Brain — global knowledge swarm hub (Cloudflare Worker)",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  }
}
